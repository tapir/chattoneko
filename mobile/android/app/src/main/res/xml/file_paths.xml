<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Share writes to Directory.Cache (context.cacheDir) before handing the
         uri to the FileProvider; nothing in the app touches external storage,
         and an <external-path> here would expose all of it. -->
    <cache-path name="my_cache_images" path="." />
</paths>