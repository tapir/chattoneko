{
  "name": "chattoneko-mobile",
  "private": true,
  "version": "0.1.0",
  "description": "Chattoneko Android app — Capacitor wrapper around the web frontend; server address is configured in-app.",
  "scripts": {
    "icons": "node scripts/icons.mjs",
    "sync": "node scripts/icons.mjs && npm --prefix ../web run build && npx cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/app": "^8.0.0",
    "@capacitor/camera": "^8.2.4",
    "@capacitor/core": "^8.0.0",
    "@capacitor/status-bar": "^8.0.3",
    "@capawesome/capacitor-file-picker": "^8.1.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0"
  }
}
